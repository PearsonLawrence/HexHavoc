//additions that need to be made to the spell manager in order for it to interact with the audio manager
//Written by Braden Turner

 //reference audio
   /* public AudioManager audioManager;

    private void Start()
    {
        // Find the AudioManager GameObject if not already set
        if (audioManager == null)
        {
            audioManager = FindObjectOfType<AudioManager>();
            if (audioManager == null)
            {
                Debug.LogError("AudioManager not found in the scene!");
            }
        }
    }

//Fire attack (add to where the method 2 cast a fire ball is, same with the rest of the spells)

audioManager.PlayFireballSound();

//wind attack

audioManager.PlayWindBlastSound();

//water attack

audioManager.PlayWaterShotSound();

//earth attack

audioManager.PlayEarthSpearSound();

//fire defense

audioManager.PlayFireWallSound();

//wind defense

audioManager.PlayWindWallSound();

//water defense

audioManager.PlayWaterWallSound();

//earth defense

audioManager.PlayEarthWallSound();


*/